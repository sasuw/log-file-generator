{
  "name": "log-file-generator",
  "version": "1.0.5",
  "description": "Generates a mock log file in the specified path and appends a new line to it in a specified interval until stopped.",
  "bin": {
    "log-file-generator": "./bin/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sasuw/log-file-generator"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["log", "generator"],
  "author": "Sasu Welling",
  "license": "MIT",
  "dependencies": {
    "chalk": "4.1.2",
    "command-line-args": "^5.2.1",
    "command-line-usage": "^6.1.2",
    "node-cleanup": "^2.1.2"
  }
}
